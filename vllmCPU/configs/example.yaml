model: <required -> your model path>
served_model_name: <optional -> your model name>
# hostは0.0.0.0を明記しないとコンテナ外からアクセスできない
host: 0.0.0.0
port: <optional -> your server port>
api-key: <optional -> your model's api key>

# 私の例を下記に示します。
# model: /app/vllm/models/gemma-3-1b
# served_model_name: gemma-3-1b
# host: 0.0.0.0
# port: 9000